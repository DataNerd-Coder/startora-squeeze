# Startora Squeeze Page

A standalone HTML landing page for Startora's 25-month anniversary business-registration offer.

## Run locally

Open `Startora_squeeze.html` in a browser, or serve the directory with any static HTTP server:

```bash
python3 -m http.server 8000
```

The page uses Google Fonts and Google Forms / Apps Script endpoints configured in the HTML.
